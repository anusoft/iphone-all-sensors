# Intermediate captures — regenerated by capture.sh (not for upload)
raw/*.png
raw/**/*.png
!raw/.gitkeep

# Local simulator build output
.build/

# macOS cruft
.DS_Store

# NOTE: final, upload-ready images in iphone-6.9/ and ipad-13/ are intentionally
# NOT ignored — commit them if you want the store assets versioned, or add
#   iphone-6.9/*.png
#   ipad-13/*.png
# here if you'd rather keep them local-only.
